---
description: Update project documentation and save conversation summary
---

// turbo-all

1. Update `CHANGELOG.md` with recent project modifications.
2. Update `ROADMAP.md` marking completed phases and adding new ones.
3. Update `README.md`, `TECH_STACK.md`, `INCIDENT_LOG.md`, and `prompt.md` as necessary.
4. Create a conversation summary file in `docs/conversations/summary_<date>_<topic>.md`.
5. Ensure `docs/conversations/` is listed in `.gitignore` to keep summaries local.
